Here's how to install the libraries needed to build these, on Debian/Ubuntu/Mint

AGAR
Precompiled packages for Windows and MacOS X are available at
http://libagar.org/download.html while on Unix you install it from source:
    wget http://stable.hypertriton.com/agar/agar-1.5.0.tar.gz
    tar xfz agar*tgz
    cd agar*
    ./configure
    make depend all
    sudo make install

ELM/EVAS
    apt-get install libelementary-dev

FLTK
    apt-get install libfltk1.3-dev

GTK2
    apt-get install libgtk2.0-dev

GTK3
    apt-get install libgtk-3-dev

IUP
    Install it from source code. See INSTALL-IUP

QT4
    apt-get install libqt4-dev

SDL1
    apt-get install libsdl1.2-dev

SDL2
    apt-get install libsdl2-dev

make
make show	# Launches all target programs
